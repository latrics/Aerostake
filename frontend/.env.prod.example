# ==============================================================================
# AEROSTAKE FRONTEND — PRODUCTION ENVIRONMENT TEMPLATE (.env.prod.example)
# Use this configuration in your production VPS (.env.prod)
# ==============================================================================

# Production Backend API Live Domain
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NODE_ENV=production
